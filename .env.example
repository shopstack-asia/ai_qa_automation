# Optional: public app URL (for OAuth redirect when behind proxy). Example: https://qa.shopstack.asia
# APP_URL=

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/qa_platform"

# Redis (BullMQ)
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET="your-32-char-minimum-secret-for-signing-tokens"

# Encryption for credentials (32+ chars or 64-char hex)
ENCRYPTION_KEY="your-32-char-encryption-key-here"

# OpenAI, S3: configure in the app Config page (admin), or set here as fallback.
# N8N API keys: managed in Config → API Keys (N8N).

# Worker
WORKER_CONCURRENCY="2"
